# Development & CI-only dependencies.
#
# Production deps live in `requirements.txt` and are pinned at the versions
# shipped on Railway. This file holds the additional packages needed to run
# the test suite (pytest + plugins) and any local-dev tooling.
#
# CI pulls both files:
#   pip install -r requirements.txt -r requirements-dev.txt

pytest==8.3.4
pytest-mock==3.15.1
pytest-timeout==2.4.0
